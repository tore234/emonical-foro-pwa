import OpenAI from "openai";

// Por ejemplo, si solo usas tu backend:
const BASE_URL = "https://armony-backend.onrender.com/chat";

// o si algún día usas env (solo en backend, no en front):
// const OPENAI_API_KEY = process.env.OPENAI_API_KEY;

export default openai;
